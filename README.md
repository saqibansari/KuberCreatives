
  # Build design agency website

  This is a code bundle for Build design agency website. The original project is available at https://www.figma.com/design/qurtMgTjbSIuuu2KCPNg4A/Build-design-agency-website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  